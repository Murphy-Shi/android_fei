// In the asset pack’s build.gradle file:
apply plugin: 'com.android.asset-pack'

assetPack {
    packName = "game_assets" // Directory name for the asset pack
    dynamicDelivery {
        deliveryType = "install-time"
    }
}